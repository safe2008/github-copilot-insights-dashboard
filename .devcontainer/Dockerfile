# Minimal devcontainer base. Node 26 is installed by the `node` feature pinned in
# devcontainer.json (Microsoft's typescript-node image has no :26 tag yet).
FROM mcr.microsoft.com/devcontainers/base:bookworm
